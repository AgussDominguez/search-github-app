import {StyleSheet} from 'react-native';

export const UsersPageTemplateStyles = StyleSheet.create({
  container: {
    display: 'flex',
    width: '100%',
    height: '100%',
    justifyContent: 'center',
  },
});
